{
  "name": "ip-address-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "A web application that geolocates any IP address or domain and displays the result on an interactive map.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6"
  }
}
